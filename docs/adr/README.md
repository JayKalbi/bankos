# Architecture Decision Records (ADR)

This folder contains the authoritative log of architectural decisions for BankOS. 

We use the [MADR](https://adr.github.io/madr/) format. Decisions made here are immutable; once accepted, they can only be changed by creating a new ADR that supersedes the previous one.
