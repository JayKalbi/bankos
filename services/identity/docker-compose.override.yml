version: '3.8'
services:
  identity:
    build: .
    ports:
      - "8081:8080"
    environment:
      - LOG_LEVEL=DEBUG
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/identity_db
      - JWT_SECRET=dev-secret-key