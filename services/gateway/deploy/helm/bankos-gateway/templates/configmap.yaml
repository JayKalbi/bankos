apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bankos-gateway.fullname" . }}-config
  labels:
    {{- include "bankos-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
